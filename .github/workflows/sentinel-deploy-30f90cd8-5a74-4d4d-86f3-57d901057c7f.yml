name: Deploy Content to xuhu-test-euap [30f90cd8-5a74-4d4d-86f3-57d901057c7f]
# Note: This workflow will deploy everything in the root directory.
# To deploy content only from a specific path (for example SentinelContent):
#   1. Add the target path to the "paths" property like such
#    paths:
#    - 'SentinelContent/**'
#    - '!.github/workflows/**'
#    - '.github/workflows/sentinel-deploy-30f90cd8-5a74-4d4d-86f3-57d901057c7f.yml'
#   2. Append the path to the directory environment variable below
#       directory: '${{ github.workspace }}/SentinelContent'

on:
  push:
    branches: [ main_SentinelExport_1676594102 ]
    paths:
    - '**'
    - '!.github/workflows/**'  # this filter prevents other workflow changes from triggering this workflow
    - '.github/workflows/sentinel-deploy-30f90cd8-5a74-4d4d-86f3-57d901057c7f.yml'

jobs:
  deploy-content:
    runs-on: windows-latest
    env:
      resourceGroupName: 'enes-testresourcegroup'
      workspaceName: 'xuhu-test-euap'
      workspaceId: 'ad22f9fa-fa9b-41a7-b2d3-a8cd2019b6a3'
      directory: '${{ github.workspace }}'
      cloudEnv: 'AzureCloud'
      contentTypes: 'AnalyticsRule'
      branch: 'main_SentinelExport_1676594102'
      sourceControlId: '30f90cd8-5a74-4d4d-86f3-57d901057c7f'
      rootDirectory: '${{ github.workspace }}'
      githubAuthToken: ${{ secrets.GITHUB_TOKEN }}
      smartDeployment: 'true'
    permissions:
      contents: write
      id-token: write # Require write permission to Fetch an OIDC token.

    steps:
    - name: Login to Azure (Attempt 1)
      continue-on-error: true
      id: login1
      uses: azure/login@v2
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_30f90cd85a744d4d86f357d901057c7f }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_30f90cd85a744d4d86f357d901057c7f }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_30f90cd85a744d4d86f357d901057c7f }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 1 failed
      if: ${{ steps.login1.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 2)
      continue-on-error: true
      id: login2
      uses: azure/login@v2
      if: ${{ steps.login1.outcome=='failure' }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_30f90cd85a744d4d86f357d901057c7f }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_30f90cd85a744d4d86f357d901057c7f }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_30f90cd85a744d4d86f357d901057c7f }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 2 failed
      if: ${{ steps.login2.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 3)
      continue-on-error: false
      id: login3
      uses: azure/login@v2
      if: ${{ steps.login2.outcome=='failure'  }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_30f90cd85a744d4d86f357d901057c7f }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_30f90cd85a744d4d86f357d901057c7f }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_30f90cd85a744d4d86f357d901057c7f }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Checkout
      uses: actions/checkout@v3

    - name: Deploy Content to Microsoft Sentinel
      uses: azure/powershell@v2
      with:
        azPSVersion: 'latest'
        inlineScript: |
          ${{ github.workspace }}//.github/workflows/azure-sentinel-deploy-30f90cd8-5a74-4d4d-86f3-57d901057c7f.ps1